<Window x:Class="SupermarketWPF.View.ProduseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SupermarketWPF.View"
        mc:Ignorable="d"
        Title="ProduseView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <DataGrid ItemsSource="{Binding Produse}" SelectedItem="{Binding SelectedProdus}" AutoGenerateColumns="True" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Adaugă" Command="{Binding AddCommand}" Width="100" Margin="5" />
                <Button Content="Modifică" Command="{Binding UpdateCommand}" Width="100" Margin="5" />
                <Button Content="Șterge" Command="{Binding DeleteCommand}" Width="100" Margin="5" />

                <ListBox ItemsSource="{Binding Produse}" DisplayMemberPath="NumeProdus" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
